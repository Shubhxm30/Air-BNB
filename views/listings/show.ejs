<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
</head>
<body>
    <h3>Listing Details :</h3>
     <ul>
        <li> <%= listings.title %> </li>
        <li> <%= listings.description %> </li>
        <li> &#x20B9; <%= listings.price.toLocaleString("en-IN") %> </li>
        <li> <%= listings.location %> </li>
        <li> <%= listings.country %> </li>
     </ul>
     <br>
     <a href="/lists/<%= listings._id %>/edit">Edit This List</a>
     <br><br>
     <form method="POST" action="/lists/<%= listings._id %>?_method=DELETE">
        <button>Delete</button>
    </form>
</body>
</html>